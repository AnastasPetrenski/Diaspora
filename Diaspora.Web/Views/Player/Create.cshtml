@model Diaspora.Web.ViewModels.Player.CreatePlayerViewModel

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
   
}

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-controller="Player" asp-action="Create" method="post">
           
            <div class="form-group">
                <label asp-for="PlayerName" class="control-label">PlayerName</label>
                <input asp-for="PlayerName" class="form-control" name="PlayerName"/>
            </div>
           
            <div class="form-group">
                <button type="submit" class="btn btn-success">Create</button>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
